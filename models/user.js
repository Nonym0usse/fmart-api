class User {
    constructor(uid, username, mail, phone, language) {
            this.uid = uid;
            this.username = username;
            this.mail = mail;
            this.phone = phone;
            this.language = language
    }
}

module.exports = User;